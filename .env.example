# Database Connection URL (SQLite for local dev, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# JWT Secret Key for Authentication Tokens
JWT_SECRET="super-secret-jwt-key-pg-manager-change-in-prod"

# Next.js Environment
NODE_ENV="production"
